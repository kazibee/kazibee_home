name: Deploy Dev — Website API and Coordinator

# Main Website: login, executor enrollment, APIs and ExecutorCoordinator.
# Target: kazibee-dev at dev.kazibee.com. No Agent or MCP deployment here.

on:
  push:
    branches:
      - release-candidate
  workflow_dispatch:

concurrency:
  group: deploy-worker-dev
  cancel-in-progress: false

jobs:
  deploy-worker:
    runs-on: self-hosted
    # CLOUDFLARE_API_TOKEN lives as an ENVIRONMENT secret (GitHub
    # Environments are the dev/prod boundary): jobs only receive
    # environment secrets when they declare the environment. `dev` for
    # now; a `production` environment (own token + protection rules) can
    # gate the release-branch deploy later.
    environment: dev

    steps:
      - name: Set temporary npm configuration paths
        run: |
          echo "NPM_INTERNAL_AUTH_FILE=$RUNNER_TEMP/npm-internal-auth.npmrc" >> "$GITHUB_ENV"
          echo "NPM_NO_AUTH_FILE=$RUNNER_TEMP/npm-no-auth.npmrc" >> "$GITHUB_ENV"
      - name: Checkout website
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Gate on internal registry credential
        env:
          KAZIBEE_INTERNAL_NPM_TOKEN: ${{ secrets.KAZIBEE_INTERNAL_NPM_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${KAZIBEE_INTERNAL_NPM_TOKEN:-}" ]; then
            echo "::error::KAZIBEE_INTERNAL_NPM_TOKEN is not set for environment 'dev'; cannot install @kazibee-internal/web-agent-renderer"
            exit 1
          fi
      - name: Configure job-temporary npm auth (scope only)
        env:
          KAZIBEE_INTERNAL_NPM_TOKEN: ${{ secrets.KAZIBEE_INTERNAL_NPM_TOKEN }}
        run: |
          set -euo pipefail
          umask 077
          rm -f "$NPM_INTERNAL_AUTH_FILE" "$NPM_NO_AUTH_FILE"
          printf '%s\n' \
            '@kazibee-internal:registry=https://registry.kazibee.com/' \
            "//registry.kazibee.com/:_authToken=${KAZIBEE_INTERNAL_NPM_TOKEN}" \
            > "$NPM_INTERNAL_AUTH_FILE"
          : > "$NPM_NO_AUTH_FILE"
          echo "NPM_CONFIG_USERCONFIG=$NPM_INTERNAL_AUTH_FILE" >> "$GITHUB_ENV"
      - name: Install dependencies
        run: npm ci --no-audit
      - name: Remove registry auth before staging and deploy
        if: always()
        run: |
          set -euo pipefail
          rm -f "$NPM_INTERNAL_AUTH_FILE"
          echo "NPM_CONFIG_USERCONFIG=$NPM_NO_AUTH_FILE" >> "$GITHUB_ENV"


      - name: Build CSS (Tailwind)
        run: npm run build:css

      - name: Build Cloudflare worker (SSR pages + API)
        run: npx noego build --target cloudflare --nodejs-compat

      # Migrations are NOT run here: they are manual-only via the
      # migrate-db-* workflows (workflow_dispatch). Deploys assume the
      # schema is already in place.

      # App secrets: SSM (us-east-1, /kazibee_web/dev/...) is the source of
      # truth; scripts/sync-secrets-from-ssm.sh copies them into the GitHub
      # `dev` environment. Deploys read only GitHub secrets — no AWS lookups
      # at deploy time.
      - name: "Deploy with wrangler (env: dev)"
        uses: cloudflare/wrangler-action@v3
        # --env dev on the command selects the [env.dev] wrangler section
        # (worker `kazibee-dev`, dev.kazibee.com); the separate
        # `environment: dev` input scopes the `wrangler secret put` calls
        # so the secrets below land on the dev worker. Never deploy the
        # top-level toml section — it is a placeholder only.
        # Runtime secrets live in the Cloudflare secret store; the action
        # uploads them right after the deploy:
        #   AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY -> S3 downloads bucket (kazibee_deploy)
        #   DATABASE_URL -> worker Postgres (environment secret on `dev`)
        #   GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET -> environment secrets (synced from SSM)
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          SWARM_AWS_ACCESS_KEY_ID: ${{ secrets.SWARM_AWS_ACCESS_KEY_ID }}
          SWARM_AWS_SECRET_ACCESS_KEY: ${{ secrets.SWARM_AWS_SECRET_ACCESS_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          KAZIQUERY_INGEST_KEY: ${{ secrets.KAZIQUERY_INGEST_KEY }}
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: dist/cloudflare
          environment: dev
          command: deploy --env dev
          secrets: |
            AWS_ACCESS_KEY_ID
            AWS_SECRET_ACCESS_KEY
            SWARM_AWS_ACCESS_KEY_ID
            SWARM_AWS_SECRET_ACCESS_KEY
            DATABASE_URL
            GOOGLE_CLIENT_ID
            GOOGLE_CLIENT_SECRET
            KAZIQUERY_INGEST_KEY

      - name: Cleanup npm auth files
        if: always()
        run: rm -f "$NPM_INTERNAL_AUTH_FILE" "$NPM_NO_AUTH_FILE"
