/**
 * Compatibility reexport. The swarm head ↔ Kazibee cloud runtime-control
 * channel contract (`swarm_head_v1`) is owned by
 * `@kazibee-internal/swarm-head-protocol`; this Website path only preserves
 * existing relative imports. Add nothing here — change the package instead.
 */
export * from "@kazibee-internal/swarm-head-protocol";
